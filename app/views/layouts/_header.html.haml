.header
  .header__logo
    %p.header__logo__font
      = icon("far", "calendar-alt")
      TECH-BOARD
  .header__menu
    .header__menu-1
      = icon("fas", "chevron-right")
      = link_to 'Home', root_path
    .header__menu-2
      - @plans = Plan.all
      = icon("fas", "chevron-right")
      = link_to "Allplans", plan_plans_all_path(@plans), method: :get
      
    .header__menu-3
      = icon("fas", "user-circle")
      = link_to "Mypage", user_path(id: current_user.id), method: :get


